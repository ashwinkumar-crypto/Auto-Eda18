pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
fg-data-profiling
Jinja2>=3.1.0
chardet>=5.0.0
scipy>=1.10.0
streamlit>=1.36.0
openpyxl>=3.1.0
xlrd>=2.0.0
pyarrow>=14.0.0
xgboost>=2.0.0
lightgbm>=4.0.0

# Optional: enables the SHAP summary plot in the ML diagnostics section.
# The app/CLI work fully without it (the plot is just skipped).
# shap>=0.45.0
xgboost>=2.0.0
lightgbm>=4.0.0
# catboost is optional and auto-detected if installed; omitted by
# default since it adds a heavy compiled dependency.
